{
  "name": "template-adonis-react-inertia",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "nx exec -- dev",
    "build": "nx exec -- build"
  },
  "dependencies": {},
  "devDependencies": {
    "nx": "15.8.9",
    "prettier": "^2.8.7"
  }
}
